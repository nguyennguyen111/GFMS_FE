import React from "react";
import "./PTPortalPages.css";

const PTClients = () => {
  return (
    <div className="ptp-wrap">
      <div className="ptp-card">
        <h2>Danh sách học viên</h2>
        <p>UI placeholder. Nối API sau (theo trainerId).</p>
      </div>
    </div>
  );
};

export default PTClients;
